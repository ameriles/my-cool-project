# my-cool-project

This is a project that does cool things.

## Building the project

Create the build directory:

    mkdir build
    cd build

Run CMake:
  
      cmake ..
      cmake --build .

## Running the project
  
      ./HelloWorld
